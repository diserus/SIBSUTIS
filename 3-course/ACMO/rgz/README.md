# РГР: Решение задачи линейного программирования

**Дисциплина:** Алгоритмы и вычислительные методы оптимизации  
**Группа:** ИП312  
**Вариант:** 2 — **Метод искусственного базиса**

---
Язык golang

## Задание

Написать программу, решающую задачу линейного программирования (ЗЛП), заданную в **канонической форме**, методом искусственного базиса.

### Каноническая форма задачи

Система ограничений:

```
a₁₁x₁ + a₁₂x₂ + ... + a₁ₙxₙ = b₁
...
aₘ₁x₁ + aₘ₂x₂ + ... + aₘₙxₙ = bₘ
x₁, x₂, ..., xₙ ≥ 0
```

Целевая функция:

```
Z = c₁x₁ + c₂x₂ + ... + cₙxₙ → max
```

### Входные данные

Считываются из файла в виде матрицы размера **(m+1) × (n+1)**:
- строки `1..m` — коэффициенты ограничений + правая часть (`b`)
- строка `m+1` — коэффициенты целевой функции

---

## Требования к программе (оценка **хорошо**)

- [x] Программа работает с классом **простых дробей** (рациональная арифметика)
- [x] Программа находит решение ЗЛП **методом искусственного базиса**
- [x] Входные данные — матрицы, удовлетворяющие требованиям метода
- [x] Программа обрабатывает случай **отсутствия решений** (несовместная система)
- [x] Если решений **бесконечно много** — программа находит **конечное число решений** (больше одного)

---

## Структура отчёта

1. Титульный лист
2. Задание варианта
3. Описание метода искусственного базиса и используемые формулы
4. Результаты работы программы на нескольких тестах (скриншоты)
5. Исходный код программы

---

## Критерии защиты

Для получения оценки **хорошо** необходимо выполнить требования выше и пройти **4 из 5** пунктов защиты:

1. Проверка работоспособности программы
2. Соответствие требованиям на желаемую оценку
3. Самостоятельность написания кода *(по желанию преподавателя)*
4. Знание метода — **теоретическая** проверка
5. Знание метода — **практическая** проверка
